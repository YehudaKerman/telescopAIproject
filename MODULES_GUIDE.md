# מדריך מודולים — TelescopeAI

הסבר בעברית על מה עושה כל קובץ בפרויקט.

---

## נקודות כניסה

**[main.py](main.py)** — נקודת הכניסה הראשית למחשב Windows. מציג תפריט אינטראקטיבי שמאפשר לשלוט בטלסקופ: להפעיל מעקב אחרי מטרה נעה, לכוון לקורדינטת GPS, לכוון לצפון, או לעקוב אחרי גרם שמיים (ירח, מאדים וכו') תוך שימוש ב-Skyfield לחישוב המיקום.

**[main_rpi.py](main_rpi.py)** — נקודת הכניסה של הראספברי פיי. מפעיל את כל צינור המעקב מקומית (ללא מסך) ובמקביל מפעיל שרת סטרימינג שמשדר את הפריימים המעובדים לרשת. מטפל בסיגנלים לסגירה נקייה.

**[main_remote.py](main_remote.py)** — צופה מרחוק שרץ על Windows. מתחבר לסטרים של הראספברי פיי דרך HTTP ומציג את הוידאו המלא עם שכבת המעקב על המסך דרך OpenCV.

**[calibration.py](calibration.py)** — אשף כיול דו-שלבי: תחילה מחשב את קנה המידה (כמה מעלות לפיקסל) וסיבוב המצלמה יחסית לציר ה-Az, ואז מיישר את השעון הפנימי של הטלסקופ מול נקודות ציון ידועות בשטח.

---

## Core — ליבת המערכת

**[core/tracker.py](core/tracker.py)** — לב המערכת. מריץ לולאה מתמשכת שבה כל פריים נבדק לתנועה: במצב סריקה הוא מזהה אובייקטים נעים דרך הפרשי פריימים, ובמצב מעקב הוא נועל עם אלגוריתם CSRT ומזין את השגיאה ב-PID שמפיק קצב תנועה לציר.

**[core/pid_controller.py](core/pid_controller.py)** — בקר PID קלאסי עם הגבלת פלט ומניעת windup. מקבל שגיאה במעלות ומחזיר קצב תנועה בדגמ"ש. קיים מופע נפרד לכל ציר (אזימוט וגובה).

**[core/session.py](core/session.py)** — מנהל הסשן ברמה הגבוהה. מחזיק את חיבורי הטלסקופ והמצלמה, מבצע בדיקות בטיחות לפני סיבוב (למשל גובה מינימלי), ומספק ממשק נקי לפקודות מ-main.py.

---

## Hardware — דרייברי טלסקופ

**[hardware/base.py](hardware/base.py)** — מחלקה מופשטת (ABC) שמגדירה את הממשק האחיד לכל דרייבר טלסקופ: חיבור, ניתוק, סיבוב, תנועת ציר, סנכרון. כל דרייבר קונקרטי חייב לממש אותה.

**[hardware/ascom_driver.py](hardware/ascom_driver.py)** — דרייבר לWindows דרך פרוטוקול ASCOM. מתקשר עם תוכנת CPWI של Celestron דרך COM object ומתרגם את הקריאות לממשק הכללי.

**[hardware/indi_driver.py](hardware/indi_driver.py)** — דרייבר לראספברי פיי דרך פרוטוקול INDI. מתחבר ל-indiserver דרך TCP, שולח ומקבל properties (מספרים, מתגים), ומתרגם אותם לממשק הכללי.

**[hardware/mock_driver.py](hardware/mock_driver.py)** — דרייבר מדומה לבדיקות. שומר בזיכרון את כל הפקודות שנשלחו אליו ומאפשר לבדוק את הלוגיקה ללא חומרה פיזית.

---

## Camera — דרייברי מצלמה

**[camera/base.py](camera/base.py)** — מחלקה מופשטת למצלמות. מגדירה `open`, `release`, `read()` שמחזיר פריים BGR ומאפיינים `width`, `height`.

**[camera/opencv_camera.py](camera/opencv_camera.py)** — דרייבר למצלמת USB רגילה דרך OpenCV. עובד עם כל webcam או מצלמה שמוכרת כ-V4L2 / DirectShow.

**[camera/zwo_camera.py](camera/zwo_camera.py)** — דרייבר למצלמת ZWO ASI183MC. משתמש ב-SDK של ZWO, מגדיר חשיפה ורגישות, ממיר את הפלט RGB24 ל-BGR לצורך תאימות עם OpenCV.

**[camera/mock_camera.py](camera/mock_camera.py)** — מצלמה סינטטית שמייצרת פריימים עם נקודה זוהרת שזזה בנתיב Lissajous. מאפשרת לבדוק את ה-PID וה-tracker בלי מצלמה אמיתית.

---

## RPi — סטרימינג

**[rpi/stream_server.py](rpi/stream_server.py)** — שרת MJPEG על Flask. פועל ב-thread נפרד, מוציא פריימים מ-queue, דוחס אותם ל-JPEG, ומשדר אותם כ-multipart stream על פורט 8080. כל לקוח שמתחבר ל-`/video` מקבל וידאו חי.

---

## Utils — תשתית

**[utils/config.py](utils/config.py)** — טוען ושומר את `config.yaml`. נקודת אמת אחת לכל הגדרות המערכת.

**[utils/logger.py](utils/logger.py)** — מגדיר logging אחיד לכל המודולים: console ברמת INFO וקובץ לוג ברמת DEBUG. מדכא לוגים רועשים ממודולים חיצוניים.

**[utils/geometry.py](utils/geometry.py)** — חישובים גיאומטריים: ממיר קורדינטות GPS לזווית אזימוט וגובה, כולל תיקון רפרקציה אטמוספרית. משמש את calibration.py ליישור הטלסקופ מול נקודות ציון.
