# TelescopeAI — Python dependencies
# Install with: pip install -r requirements.txt
#
# Optional extras:
#   ZWO ASI camera:  pip install zwoasi
#   INDI (Linux/RPi): pip install pyindi-client

# Computer vision and tracking
opencv-python>=4.8
numpy>=1.24

# Astronomy / ephemeris
skyfield>=1.46

# Configuration
pyyaml>=6.0

# Testing
pytest>=8.0

# ASCOM telescope driver — Windows only
# pywin32>=306

# ZWO ASI camera — optional
# zwoasi>=0.0.22

# INDI client — Linux / Raspberry Pi only
# pyindi-client>=1.9
